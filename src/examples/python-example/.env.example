# Project secrets — copy to `.env` and fill in. NEVER commit the real .env.
#
# llm-docker forwards BUILDER_API_PASSWORD into the container so the bundled
# MCP servers can hit the host-side builder-api without env hunting. The same
# value must match `[defaults].password` in ~/.llm-docker/builder-api.toml.

BUILDER_API_PASSWORD=
